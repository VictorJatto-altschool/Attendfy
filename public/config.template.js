// Copy this file to public/config.js and fill in your Firebase project keys for local testing only.
// IMPORTANT: public/config.js is ignored by git. For production, the GitHub Actions workflow
// generates public/config.js from encrypted repository secrets at deploy time.
window.firebaseConfig = {
  enabled: false, // set to true only in your local untracked public/config.js
  apiKey: "REPLACE",
  authDomain: "REPLACE",
  projectId: "REPLACE",
  storageBucket: "REPLACE",
  messagingSenderId: "REPLACE",
  appId: "REPLACE",
}
